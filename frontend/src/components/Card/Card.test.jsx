import Display from "./Card";
import { render } from "@testing-library/react";
import { describe, it, expect } from "vitest";

describe("Display Card", () => {
  it("Card displays the correct data", () => {
    render(<Display />);

    expect;
  });
});
